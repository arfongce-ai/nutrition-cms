# 칼로리 앱 백업 및 재개 정보

- 백업 ID: `calorie-app-20260918-1230`
- 백업 날짜: 2026-09-18
- 기준 브랜치: `codex/calorie-app-20260918-1230`
- 목적: 현재까지의 칼로리 앱 작업을 보존하고, 다른 작업 ID에서 이어서 진행하기 위한 재개 지점

## 현재 상태

- `npm.cmd test` 통과: 33개
- `npm.cmd run build` 통과
- Firebase 설정은 `.env`/`.env.local` 환경변수로 관리하며 백업에 비밀키를 포함하지 않음
- 앱은 로그인 화면 없이 동작하고, Firebase가 설정되면 익명 인증으로 식단을 저장함
- Cloudflare Pages 프로젝트명과 D1 데이터베이스 ID는 현재 운영 설정을 유지함

## 복구 및 재개

1. 백업 압축 파일을 새 작업 폴더에 풉니다.
2. `npm.cmd ci`로 의존성을 다시 설치합니다.
3. 필요한 경우 `.env.example`을 복사해 새 환경변수 파일을 만들고, 새 Firebase/Cloudflare 계정의 ID를 입력합니다.
4. `npm.cmd test`와 `npm.cmd run build`를 실행합니다.
5. 이후 작업은 이 문서의 백업 ID 또는 브랜치 `codex/calorie-app-20260918-1230`을 기준으로 이어갑니다.

## 백업에서 제외한 항목

- `node_modules/`: `npm.cmd ci`로 재생성 가능
- `dist/`: `npm.cmd run build`로 재생성 가능
- `.wrangler/`, `.qa/`: 로컬 실행·검증용 생성물
- `data/imported/`: 대용량 D1 시드 생성물. 필요한 경우 원본 작업 폴더에서 별도 보존하며, 앱 소스 복구에는 필수 아님

실제 운영 데이터와 Firebase/Cloudflare 계정 데이터는 코드 압축 백업과 별도로 해당 서비스의 공식 export/backup 기능으로 보존해야 합니다.
